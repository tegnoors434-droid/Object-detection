# Object-detection
A computer vision project built with Python and OpenCV that detects people in images using the HOG (Histogram of Oriented Gradients) descriptor. The system identifies human figures, draws bounding boxes, displays the total number of people detected, and saves the processed output image automatically.
